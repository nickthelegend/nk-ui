import React from 'react';
import { ViewStyle } from 'react-native';
interface CardProps {
    children: React.ReactNode;
    style?: ViewStyle;
    elevated?: boolean;
    padding?: number;
    noBorder?: boolean;
}
export declare function Card({ children, style, elevated, padding, noBorder }: CardProps): any;
export {};
//# sourceMappingURL=card.d.ts.map